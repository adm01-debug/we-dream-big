/**
 * useColors — alias do hook useColorSystem para compatibilidade com código legado/documentação.
 * Mantém uma API estável: retorna a lista de cores normalizadas do sistema.
 */
export { useColorSystem as useColors } from "./useColorSystem";
