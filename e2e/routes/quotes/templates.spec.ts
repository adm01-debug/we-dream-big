import { buildAuthedRouteSuite } from "../_factories";
buildAuthedRouteSuite({ name: "/orcamentos/templates", path: "/orcamentos/templates", primary: { kind: "rest", key: "quote_templates", successBody: [] } });
