import { buildAuthedRouteSuite } from "../_factories";
buildAuthedRouteSuite({ name: "/admin/video-variantes", path: "/admin/video-variantes", primary: { kind: "rest", key: "product_videos", successBody: [] } });
