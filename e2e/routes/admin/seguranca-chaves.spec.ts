import { buildAuthedRouteSuite } from "../_factories";
buildAuthedRouteSuite({ name: "/admin/seguranca/chaves", path: "/admin/seguranca/chaves", primary: { kind: "rest", key: "mcp_keys", successBody: [] } });
