import { buildAuthedRouteSuite } from "../_factories";
buildAuthedRouteSuite({ name: "/mockups/historico", path: "/mockups/historico", primary: { kind: "rest", key: "mockup_history", successBody: [] } });
