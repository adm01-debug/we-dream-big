import { buildAuthedRouteSuite } from "../_factories";
buildAuthedRouteSuite({ name: "/ferramentas/bi/comparar", path: "/ferramentas/bi/comparar", primary: { kind: "fn", key: "external-db-bridge", successBody: { rows: [] } } });
