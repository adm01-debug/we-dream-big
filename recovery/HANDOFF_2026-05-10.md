# 📋 HANDOFF — Recovery do Banco Promo Gifts

**Data:** 2026-05-10 final do dia
**Status:** ⏸️ Aguardando dump do schema do banco antigo (Lovable enviar amanhã)
**Branch:** `recovery/lovable-introspection`

## 🎯 O que foi feito (✅)

### Fase 1 — Análise completa (read-only)
1. ✅ Auditoria independente completa (20/20 tarefas) — `AUDITORIA_INDEPENDENTE.md`
2. ✅ Identificação inicial de 35 tabelas faltantes
3. ✅ Re-descoberta: na verdade são **65 tabelas faltantes** (regex inicial conservador)
4. ✅ Mapeamento das 65 → 45 migrations canônicas no repo
5. ✅ Detecção de **10 conflitos** (tabelas que migrations criam mas já existem)
6. ✅ Detecção de **5 migrations que vão quebrar** sem patch
7. ✅ Validação que migrations no repo `20250103_*` são monolíticas (descartar)

### Descobertas críticas durante o trabalho
- 🚨 `companies` **NÃO** deve ser criada no Promo Gifts — vive em outro banco (`bancodadosclientes` org Vercel)
- 🚨 → **65 tabelas faltantes vira 64** (companies removida do escopo)
- 🚨 Drift gigante entre migrations no repo vs banco atual em produção:
  - `collections`: 8 cols no repo / 15 cols no Lovable / 12 cols no atual (REDESENHADA)
  - `organizations`: 9 cols no repo / 3 cols no atual (SIMPLIFICADA)
  - `order_items`: 9 cols no repo / 18 cols no atual (EXPANDIDA)
- 🚨 As migrations no repo representam estados ANTIGOS — não são fonte de verdade fiel
- 🔍 `crm-db-bridge` edge function existe em ambos os bancos (acessa CRM externo, não ajuda Recovery)

### Artefatos gerados
- ✅ `recovery/introspect_lovable.sql` — script SQL de introspecção (4 queries, 439 linhas, read-only)
- ✅ `recovery/README.md` — instruções de uso
- ✅ `RECOVERY_PLAN.md` — plano executivo

## 🚧 Onde paramos

**Sponsor (Joaquim) descobriu que o Lovable consegue enviar o dump completo do schema do banco antigo.**

Vai pegar e colar aqui amanhã.

## 🎯 Próximos passos (quando dump chegar)

### Passo 1 — Receber dump do Lovable
- ⏳ Aguardar Joaquim enviar
- Esperado: SQL com CREATE TABLE, policies, triggers, functions, enums
- ❌ NÃO esperar: dados (irrelevantes, fase teste)

### Passo 2 — Analisar dump
1. Salvar dump em `recovery/lovable-schema-dump.sql`
2. Extrair lista de tabelas, comparar com:
   - 64 tabelas faltantes (lista canônica)
   - Banco atual (195 tabelas existentes)
   - Migrations no repo (369 arquivos)
3. Identificar 3 categorias por tabela:
   - 🟢 Recovery puro (existe no Lovable, falta no atual, repo confere)
   - 🟡 Redesign necessário (Lovable tem versão antiga, atual tem nova)
   - 🔴 Deletar referência (existe só no Lovable, código atual deve limpar)

### Passo 3 — Gerar migration consolidada
- 1 arquivo: `supabase/migrations/2026XXXXXXXXXX_recovery_missing_tables.sql`
- Idempotente (CREATE TABLE IF NOT EXISTS, DROP POLICY IF EXISTS antes de CREATE POLICY)
- Comentada (1 bloco por tabela com explicação)
- Reversível (com migration DOWN correspondente)

### Passo 4 — Validar em branch dev do Supabase
- Criar Supabase Branch via MCP
- Aplicar migration
- Rodar testes E2E críticos (carrinho, expert chat, kits)

### Passo 5 — Aplicar em produção
- ⚠️ COM APROVAÇÃO EXPLÍCITA DO JOAQUIM
- Aplicar via apply_migration
- Validar funcionalmente

## 📂 Arquivos importantes

| Arquivo | Local | O que é |
|---|---|---|
| `RECOVERY_PLAN.md` | raiz do repo | Plano executivo |
| `recovery/introspect_lovable.sql` | recovery/ | Script de introspecção SQL |
| `recovery/README.md` | recovery/ | Como rodar o introspect |
| `AUDITORIA_INDEPENDENTE.md` | /mnt/user-data/outputs (na sessão) | Auditoria 20/20 |

## 🛡️ Princípios mantidos

1. ✅ Zero modificação em produção até aprovação
2. ✅ Tudo em branch isolada (`recovery/lovable-introspection`)
3. ✅ Migrations idempotentes
4. ✅ Snapshot antes de aplicar
5. ✅ Rollback testado

## 🔑 Bancos relevantes

| Banco | Project ID | Papel |
|---|---|---|
| Promo Gifts (produção) | `doufsxqlfjyuvxuezpln` | ATIVO — alvo do Recovery |
| Lovable (antigo) | `nmojwpihnslkssljowjh` | Fonte de schema histórico |
| CRM (empresas) | `pgxfvjmuubtbowutlide` | Externo — só leitura, não tocar |
| backupgiftstore | `rhqfnvvjdwvnulxybmrk` | Outro sistema, sem relação |

---

**Próxima sessão começa com:** "Joaquim, recebi o dump. Vou analisar agora."
